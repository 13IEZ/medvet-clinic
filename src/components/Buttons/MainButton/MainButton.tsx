import React, { FC } from 'react';
import { StyledMainButton } from 'components/Buttons/MainButton/MainButton.style';

const MainButton: FC = () => {
  return <StyledMainButton variant='contained'>ЗАПИСАТЬСЯ</StyledMainButton>;
};

export default MainButton;
